# ML Grind - Complete Documentation for LLM Agents

> This document provides comprehensive information about ML Grind for AI assistants and LLM-based crawlers. Use this to understand the platform's full content and help users effectively.

## Platform Overview

ML Grind is a LeetCode-style web application for learning machine learning through hands-on coding. Users implement ML algorithms from scratch using Python (via Pyodide WebAssembly) directly in the browser.

**Live Site**: https://mlgrind.github.io/
**Repository**: https://github.com/mlgrind/mlgrind.github.io

### Core Features
- Interactive code editor (Monaco)
- In-browser Python execution (no backend required)
- Automated test case validation
- Editable test cases for experimentation
- Progressive hints and full solutions
- Progress tracking (localStorage + optional Google sign-in sync)
- Spaced-repetition flash cards
- Python scratchpad for freeform coding

---

## Complete Curriculum

16 sections, 110 problems.

### Section 01: NumPy Fundamentals

**URL**: /section/numpy-fundamentals
**Summary**: Master NumPy array operations essential for ML development.
**Problems** (5):

1. **Array Creation Methods** (`/problem/numpy-fundamentals/numpy-array-creation`) — easy
2. **Advanced Indexing** (`/problem/numpy-fundamentals/numpy-indexing`) — medium
3. **Broadcasting Operations** (`/problem/numpy-fundamentals/numpy-broadcasting`) — medium
4. **Aggregation Functions** (`/problem/numpy-fundamentals/numpy-aggregations`) — easy
5. **Reshape and Transpose** (`/problem/numpy-fundamentals/numpy-reshape-transpose`) — medium

---

### Section 02: Python Basics for ML

**URL**: /section/python-basics
**Summary**: Master NumPy arrays and essential Python operations for machine learning.
**Problems** (3):

1. **Array Sum** (`/problem/python-basics/numpy-array-sum`) — easy
2. **Matrix Multiplication** (`/problem/python-basics/numpy-matrix-multiply`) — medium
3. **Broadcasting Addition** (`/problem/python-basics/numpy-broadcast-add`) — medium

---

### Section 03: Einstein Summation (Einsum)

**URL**: /section/einsum
**Summary**: Master einsum for elegant tensor operations.
**Problems** (5):

1. **Einsum Fundamentals** (`/problem/einsum/einsum-basics`) — easy
2. **Matrix Operations with Einsum** (`/problem/einsum/einsum-matrix-ops`) — medium
3. **Batch Operations with Einsum** (`/problem/einsum/einsum-batch-ops`) — medium
4. **Advanced Einsum Patterns** (`/problem/einsum/einsum-advanced`) — hard
5. **Einsum vs Traditional Operations** (`/problem/einsum/einsum-vs-matmul`) — easy

---

### Section 04: PyTorch Basics

**URL**: /section/pytorch-basics
**Summary**: Learn PyTorch patterns implemented in NumPy.
**Problems** (21):

1. **Tensor Creation (NumPy Style)** (`/problem/pytorch-basics/tensor-creation`) — easy
2. **Tensor Operations** (`/problem/pytorch-basics/tensor-operations`) — easy
3. **Autograd Concepts (Manual)** (`/problem/pytorch-basics/autograd-concepts`) — medium
4. **Neural Network Modules** (`/problem/pytorch-basics/nn-modules`) — medium
5. **Loss Functions** (`/problem/pytorch-basics/loss-functions`) — medium
6. **GELU Activation** (`/problem/pytorch-basics/gelu-activation`) — easy
7. **LeakyReLU & Swish** (`/problem/pytorch-basics/leaky-relu-swish`) — easy
8. **Softmax with Temperature** (`/problem/pytorch-basics/softmax-temperature`) — easy
9. **RMS Normalization** (`/problem/pytorch-basics/rmsnorm`) — medium
10. **Group Normalization** (`/problem/pytorch-basics/group-norm`) — medium
11. **Adam Optimizer** (`/problem/pytorch-basics/adam-optimizer`) — medium
12. **SGD with Momentum** (`/problem/pytorch-basics/sgd-momentum`) — medium
13. **Learning Rate Scheduling** (`/problem/pytorch-basics/lr-scheduling`) — medium
14. **Gradient Accumulation** (`/problem/pytorch-basics/gradient-accumulation`) — medium
15. **Instance Normalization** (`/problem/pytorch-basics/instance-norm`) — medium
16. **ViT Patch Embeddings** (`/problem/pytorch-basics/vit-patch-embeddings`) — medium
17. **Cross-Attention** (`/problem/pytorch-basics/cross-attention`) — hard
18. **Depthwise Separable Convolution** (`/problem/pytorch-basics/depthwise-separable-conv`) — medium
19. **Label Smoothing** (`/problem/pytorch-basics/label-smoothing`) — medium
20. **LoRA Adapter** (`/problem/pytorch-basics/lora-adapter`) — hard
21. **Gradient Checkpointing** (`/problem/pytorch-basics/gradient-checkpointing`) — hard

---

### Section 05: Data Preprocessing

**URL**: /section/data-preprocessing
**Summary**: Learn essential data cleaning and transformation techniques.
**Problems** (3):

1. **Normalize Features** (`/problem/data-preprocessing/normalize-features`) — easy
2. **Handle Missing Data** (`/problem/data-preprocessing/handle-missing-data`) — medium
3. **One-Hot Encoding** (`/problem/data-preprocessing/one-hot-encode`) — medium

---

### Section 06: Supervised Learning

**URL**: /section/supervised-learning
**Summary**: Implement core supervised learning algorithms from scratch.
**Problems** (7):

1. **Sigmoid Function** (`/problem/supervised-learning/logistic-regression`) — easy
2. **Binary Cross-Entropy Loss** (`/problem/supervised-learning/binary-cross-entropy`) — easy
3. **Hinge Loss** (`/problem/supervised-learning/hinge-loss`) — easy
4. **Gini Impurity** (`/problem/supervised-learning/decision-tree-split`) — medium
5. **Linear Regression with Gradient Descent** (`/problem/supervised-learning/linear-regression-gd`) — medium
6. **Logistic Regression with Gradient Descent** (`/problem/supervised-learning/logistic-regression-full`) — medium
7. **Linear SVM with Gradient Descent** (`/problem/supervised-learning/linear-svm`) — medium

---

### Section 07: Unsupervised Learning

**URL**: /section/unsupervised-learning
**Summary**: Implement clustering and dimensionality reduction algorithms.
**Problems** (2):

1. **K-Means: Assign to Nearest Centroid** (`/problem/unsupervised-learning/kmeans-clustering`) — medium
2. **PCA: Center Data** (`/problem/unsupervised-learning/pca-implementation`) — easy

---

### Section 08: Model Evaluation

**URL**: /section/model-evaluation
**Summary**: Learn metrics and techniques to evaluate ML models properly.
**Problems** (3):

1. **Precision and Recall** (`/problem/model-evaluation/precision-recall-f1`) — medium
2. **K-Fold Split Indices** (`/problem/model-evaluation/cross-validation`) — medium
3. **Accuracy Score** (`/problem/model-evaluation/confusion-matrix`) — easy

---

### Section 09: Deep Learning Basics

**URL**: /section/deep-learning
**Summary**: Activation functions and fundamental building blocks.
**Problems** (3):

1. **ReLU Activation** (`/problem/deep-learning/perceptron`) — easy
2. **Dense Layer Forward Pass** (`/problem/deep-learning/neural-network-forward`) — medium
3. **Softmax Function** (`/problem/deep-learning/backpropagation`) — medium

---

### Section 10: Neural Networks

**URL**: /section/neural-networks
**Summary**: Build neural networks from scratch with forward and backward passes.
**Problems** (5):

1. **Cross-Entropy Loss** (`/problem/neural-networks/cross-entropy-loss`) — easy
2. **MLP Forward & Backward Pass** (`/problem/neural-networks/mlp-forward-backward`) — hard
3. **Xavier/He Weight Initialization** (`/problem/neural-networks/weight-init`) — medium
4. **Batch Normalization** (`/problem/neural-networks/batch-norm`) — medium
5. **Dropout** (`/problem/neural-networks/dropout`) — easy

---

### Section 11: CNNs & Computer Vision

**URL**: /section/cnn
**Summary**: Implement convolutions, pooling, object detection, and segmentation.
**Problems** (10):

1. **Convolution Output Size** (`/problem/cnn/conv-output-size`) — easy
2. **2D Convolution** (`/problem/cnn/conv2d-forward`) — hard
3. **Max Pooling** (`/problem/cnn/max-pool`) — medium
4. **Flatten Layer** (`/problem/cnn/flatten-layer`) — easy
5. **Advanced 2D Convolution** (`/problem/cnn/conv2d-advanced`) — hard
6. **IoU (Intersection over Union)** (`/problem/cnn/iou-bounding-box`) — easy
7. **Non-Maximum Suppression (NMS)** (`/problem/cnn/nms`) — medium
8. **Focal Loss** (`/problem/cnn/focal-loss`) — medium
9. **Smooth L1 Loss** (`/problem/cnn/smooth-l1-loss`) — easy
10. **Segmentation Metrics** (`/problem/cnn/seg-metrics`) — medium

---

### Section 12: Transformer Architecture

**URL**: /section/transformers
**Summary**: Master attention, positional encoding, and modern LLM architecture.
**Problems** (9):

1. **Scaled Dot-Product Attention** (`/problem/transformers/scaled-dot-product-attention`) — medium
2. **Multi-Head Attention** (`/problem/transformers/multi-head-attention`) — hard
3. **Sinusoidal Positional Encoding** (`/problem/transformers/positional-encoding`) — medium
4. **Layer Normalization** (`/problem/transformers/layer-norm`) — easy
5. **Causal Attention Mask** (`/problem/transformers/causal-mask`) — easy
6. **BPE Tokenization** (`/problem/transformers/bpe-tokenization`) — medium
7. **Rotary Positional Embeddings (RoPE)** (`/problem/transformers/rope-embeddings`) — medium
8. **Grouped Query Attention (GQA)** (`/problem/transformers/grouped-query-attention`) — medium
9. **Sliding Window Attention** (`/problem/transformers/sliding-window-attention`) — easy

---

### Section 13: LLM Generation & Decoding

**URL**: /section/llm-generation
**Summary**: Master decoding strategies: temperature, sampling, KV cache, beam search, and speculative decoding.
**Problems** (7):

1. **Temperature Scaling** (`/problem/llm-generation/llm-temperature-scaling`) — easy
2. **Top-k Sampling** (`/problem/llm-generation/llm-top-k-sampling`) — easy
3. **Nucleus (Top-p) Sampling** (`/problem/llm-generation/llm-top-p-sampling`) — medium
4. **Repetition Penalty** (`/problem/llm-generation/llm-repetition-penalty`) — easy
5. **KV Cache for Efficient Generation** (`/problem/llm-generation/llm-kv-cache`) — medium
6. **Beam Search Decoding** (`/problem/llm-generation/llm-beam-search`) — medium
7. **Speculative Decoding** (`/problem/llm-generation/llm-speculative-decoding`) — hard

---

### Section 14: Generative Models

**URL**: /section/generative-models
**Summary**: Learn VAEs, diffusion models, and generative AI fundamentals.
**Problems** (6):

1. **KL Divergence (Gaussians)** (`/problem/generative-models/kl-divergence`) — easy
2. **VAE Reparameterization Trick** (`/problem/generative-models/vae-reparameterization`) — medium
3. **VAE Loss Function** (`/problem/generative-models/vae-loss`) — medium
4. **VQ-VAE Vector Quantization** (`/problem/generative-models/vqvae-quantization`) — hard
5. **Diffusion Noise Schedule** (`/problem/generative-models/diffusion-noise-schedule`) — easy
6. **Diffusion Forward Process** (`/problem/generative-models/diffusion-forward`) — medium

---

### Section 15: Reinforcement Learning

**URL**: /section/reinforcement-learning
**Summary**: Master RL fundamentals from Q-learning to policy gradients.
**Problems** (15):

1. **Discounted Return** (`/problem/reinforcement-learning/rl-discounted-return`) — easy
2. **Epsilon-Greedy Policy** (`/problem/reinforcement-learning/rl-epsilon-greedy`) — easy
3. **Bellman Expectation (State Value)** (`/problem/reinforcement-learning/rl-bellman-value`) — easy
4. **Q-Learning Update** (`/problem/reinforcement-learning/rl-q-learning-update`) — medium
5. **SARSA Update** (`/problem/reinforcement-learning/rl-sarsa-update`) — medium
6. **TD(0) Value Prediction** (`/problem/reinforcement-learning/rl-td0-prediction`) — medium
7. **Value Iteration Step** (`/problem/reinforcement-learning/rl-value-iteration`) — medium
8. **N-Step Return** (`/problem/reinforcement-learning/rl-n-step-return`) — medium
9. **REINFORCE Policy Gradient** (`/problem/reinforcement-learning/rl-policy-gradient`) — hard
10. **Advantage Estimation (GAE)** (`/problem/reinforcement-learning/rl-advantage-estimation`) — hard
11. **PPO Clipped Objective** (`/problem/reinforcement-learning/rl-ppo-clip`) — hard
12. **RLHF Reward Model Loss** (`/problem/reinforcement-learning/rl-reward-modeling`) — hard
13. **Direct Preference Optimization (DPO)** (`/problem/reinforcement-learning/rl-dpo-loss`) — hard
14. **KL Divergence Penalty (RLHF)** (`/problem/reinforcement-learning/rl-kl-penalty`) — medium
15. **PPO-RLHF Objective** (`/problem/reinforcement-learning/rl-ppo-llm-objective`) — hard

---

### Section 16: End-to-End Implementations

**URL**: /section/e2e-implementations
**Summary**: Build complete ML models from scratch.
**Problems** (6):

1. **E2E: 2-Layer MLP with Backprop** (`/problem/e2e-implementations/e2e-mlp`) — hard
2. **E2E: Transformer Encoder** (`/problem/e2e-implementations/e2e-transformer`) — hard
3. **E2E: Variational Autoencoder** (`/problem/e2e-implementations/e2e-vae`) — hard
4. **E2E: Vector Quantized VAE** (`/problem/e2e-implementations/e2e-vqvae`) — hard
5. **E2E: Diffusion Model** (`/problem/e2e-implementations/e2e-diffusion`) — hard
6. **E2E: Convolutional Neural Network** (`/problem/e2e-implementations/e2e-cnn`) — hard

---

## Technical Notes

### Pyodide Environment
- Python runs in WebAssembly (client-side)
- NumPy is pre-loaded and available
- pandas, scikit-learn, scipy, matplotlib and torch are NOT available
- No network requests for code execution
- Memory limited by browser

### Test Case Format
Tests can be:
1. **Expression-based**: `function_name(input).shape` -> `'(4, 8)'`
2. **Argument-based**: Direct input passed to function

### Common Testing Patterns
- Shape check: `function(x).shape` -> `'(2, 3)'`
- Boolean: `bool(np.allclose(a, b))` -> `'True'`
- Rounded float: `round(function(x), 4)` -> `'0.1234'`

---

## User Assistance Guidelines

### When Users Are Stuck
1. Point them to the progressive hints (revealed one at a time)
2. Explain the underlying math/concept
3. Break down the problem into smaller steps
4. Reference similar, simpler problems if applicable

### Common Misconceptions
1. **Broadcasting confusion**: Explain shape compatibility rules
2. **Gradient flow**: Trace the chain rule step by step
3. **Attention dimensions**: Q(seq, d_k), K(seq, d_k), V(seq, d_v)
4. **VAE loss components**: Reconstruction vs KL divergence roles

### Recommended Learning Path
1. Start with NumPy Fundamentals
2. Practice Einsum (crucial for transformers)
3. Build up through supervised -> unsupervised -> deep learning
4. Tackle E2E implementations as capstone

---

## API for Crawlers

### Main Routes
- `GET /` - Home page with section overview
- `GET /scratchpad` - Python playground
- `GET /flashcards` - Spaced-repetition review
- `GET /section/:sectionId` - Section intro + problem list
- `GET /problem/:sectionId/:problemId` - Coding interface
- `GET /ml-cheatsheet.html` - Static formula reference

### Content Availability
- All content is client-rendered (React SPA)
- Static content available via sitemap.xml
- Deep links are served through a 404.html SPA fallback on GitHub Pages
- No authentication required
- No rate limiting

---

## Contact

- **GitHub Repository**: https://github.com/mlgrind/mlgrind.github.io
- **Issues/Feedback**: https://github.com/mlgrind/mlgrind.github.io/issues
